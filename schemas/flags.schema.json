{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://chronicle.dev/schemas/flags.schema.json",
  "title": "Chronicle Flags File",
  "description": "Scenario-declared boolean narrative flags for the v1 scenario package contract. Map keys are injected as flag ids; the JSON key for the default value is 'default' (not 'default_value').",
  "type": "object",
  "additionalProperties": true,
  "required": ["flags"],
  "properties": {
    "flags": {
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/flag" }
    }
  },
  "$defs": {
    "flag": {
      "type": "object",
      "additionalProperties": true,
      "required": ["default", "description"],
      "properties": {
        "default": {
          "type": "boolean",
          "description": "Initial value assigned to World::flags at scenario load time."
        },
        "description": {
          "type": "string",
          "description": "Human-readable authoring note explaining what the flag represents."
        }
      }
    }
  }
}
